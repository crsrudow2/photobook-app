Read `.lucy-gate-failures.md` for the specific errors and fix all lint, typecheck, and test failures. Then commit and push.

**Critical prerequisites before starting:**
1. **Check for merge conflicts**: The repo has a known merge conflict in the plan file (`.lucy-plan.md`). Before running any fix steps, check `git status` for conflicted files. If the plan file shows a conflict, you **must resolve it first** before proceeding—do not commit with unresolved conflicts.
2. **Understand the failures**: Read `.lucy-gate-failures.md` carefully and identify the root cause of each failure before attempting fixes. Do not assume all failures are just missing modules.

**Context from prior work:**
- The project has Prisma 7, which requires explicit client generation via a `postinstall` script in `package.json`
- If gate checks fail with "Cannot find module" errors, run `npm install` first (this triggers `postinstall` to generate the Prisma client automatically). Do **not** manually run `prisma generate`—the hook handles it.
- If `node_modules/` is missing after install, verify the install completed successfully before proceeding.
- Previous discoveries to leverage: Prisma 7 breaking changes (no `url` in schema, uses `prisma.config.ts` with adapter), Next.js 16 makes `params`/`searchParams` Promises.

**Approach:**
1. Check `git status` for conflicts. If `.lucy-plan.md` or other tracked files show conflicts, resolve them first.
2. Read `.lucy-gate-failures.md` to understand what specific errors need fixing.
3. If "Cannot find module" errors are present, run `npm install` to populate `node_modules/` and trigger Prisma client generation.
4. Run each gate check locally to verify they pass:
   - `npx tsc --noEmit` (TypeScript typecheck)
   - `npm run lint` (ESLint)
   - `npm run build` or `npm run test` if those exist
5. **Verify all commands exit with code 0 before proceeding to commit.**
6. Stage only the files that fix the gate failures (not build artifacts or `.next/` cache).
7. Commit with a clear message describing what was fixed (e.g., "fix: resolve TypeScript errors in auth flow", "fix: install dependencies to resolve missing modules").
8. Push to the remote branch.

**Do not add new features or refactor unrelated code—only fix the gate failures.** If a fix requires changes beyond the scope of the failures listed, flag it and do not proceed without clarification.
